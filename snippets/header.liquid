
<!-- BEGIN USF -->




{%- comment -%}
header-inline - design 3( logo left ) , 5( logo center ), 9( logo left,search form ), 2 (header menu split)
header-sidebar - design 1( button opend menu left ), design 6( button opend menu right - social ), design 10( search form )
header With categories menu - design 8
header Menu in bottom  - design 4 - social
header vertical  - design 7 - social
{%- endcomment -%}
{%- liquid
if template.suffix != 'config' and request.design_mode == false
  section 'title_config'
  section 'pr_item_config'
  section 'btn_config'
endif -%}

{%- if t_name == 'index' -%}<h1 class="site-header__logo t4s-d-none">{{- shop.name -}}</h1>{%- endif -%}
{%- liquid
  assign admin_sp = request.design_mode
    section "top-activity-bar"
  section 'announcement-bar'
  section 'top-bar'
  case settings.header_design 
   when 'inline' 
     section 'header-inline'
   when 'sidebar' 
     section 'header-sidebar'
   when 'bottom' 
     section 'header-bottom'
   when 'categories'  
      section 'header-categories-menu'
      if admin_sp 
      section 'header-categories' 
      endif
   when 'vertical' 
     section 'header-vertical'
  endcase
  if admin_sp and settings.header_design != 'sidebar'
  section 'mega-menu'
  endif  -%}
<!--   TTD Universal Pixel start -->
<script src="https://js.adsrvr.org/up_loader.1.1.0.js" type="text/javascript"></script> <script type="text/javascript"> ttd_dom_ready( function() { if (typeof TTDUniversalPixelApi === 'function') { var universalPixelApi = new TTDUniversalPixelApi(); universalPixelApi.init("tkhv81m", ["h0k88i3"], "https://insight.adsrvr.org/track/up"); } }); </script>
<!--   TTD Universal Pixel end -->
<script>                                                                                                       
window.onload = function() {
    var searchCanvas = document.querySelector('header');
  console.log(searchCanvas)
    if (searchCanvas && !window._usfInstantSearchInited) {
        searchCanvas.addEventListener("mouseenter", function() {
            if (window._usfInstantSearchInited)
                return;
            var inputs = document.querySelectorAll('input.t4s-mini-search__input');
          
            if (inputs.length) {
                if (window.usf && usf.settings.instantSearch.online){
                    inputs.forEach(e => usf.utils.installSearchInput(e));
                    console.log('%c USF - Instant Search inited', 'background: blue; color: white');
                }
                window._usfInstantSearchInited = 1;
            }
        })
    }
};
</script>
<!-- END USF -->

{% comment %}USF




{%- comment -%}
header-inline - design 3( logo left ) , 5( logo center ), 9( logo left,search form ), 2 (header menu split)
header-sidebar - design 1( button opend menu left ), design 6( button opend menu right - social ), design 10( search form )
header With categories menu - design 8
header Menu in bottom  - design 4 - social
header vertical  - design 7 - social
{%- endcomment -%}
{%- liquid
if template.suffix != 'config' and request.design_mode == false
  section 'title_config'
  section 'pr_item_config'
  section 'btn_config'
endif -%}

{%- if t_name == 'index' -%}<h1 class="site-header__logo t4s-d-none">{{- shop.name -}}</h1>{%- endif -%}
{%- liquid
  assign admin_sp = request.design_mode
  section 'announcement-bar'
  section 'top-bar'
  case settings.header_design 
   when 'inline' 
     section 'header-inline'
   when 'sidebar' 
     section 'header-sidebar'
   when 'bottom' 
     section 'header-bottom'
   when 'categories'  
      section 'header-categories-menu'
      if admin_sp 
      section 'header-categories' 
      endif
   when 'vertical' 
     section 'header-vertical'
  endcase
  if admin_sp and settings.header_design != 'sidebar'
  section 'mega-menu'
  endif  -%}


USF{% endcomment %}
